#Создать функции, которая будет строить лестницу, используя знаки ‘_’ и ‘#’. 
#Положительное значение обозначают, что направление лестницы направленно вверх и вниз для отрицательных значений. 

def task_16(a: int) -> str:
    s = abs(a)
    i = 0
    step = ''
    while i < s:
        if a > 0:
            step += (s - i) * '#' + i * '_' + '\n'
        elif a < 0:
            step += i * '_' + (s - i) * '#' + '\n'
        i += 1
    return step[::-1]

print(task_16(7))
print(task_16(-7))
